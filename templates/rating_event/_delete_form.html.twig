<form method="post" action="{{ path('rating_event_delete', {'idRating': rating_event.idRating}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rating_event.idRating) }}">
    <button class="btn">Delete</button>
</form>
