{% extends 'base.html.twig' %}

{% block title %}TypeEvent{% endblock %}

{% block body %}
{% block content %}
    <h1>TypeEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>TypeName</th>
                <td>{{ type_event.typeName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_event_index') }}">back to list</a>

    <a href="{{ path('type_event_edit', {'typeName': type_event.typeName}) }}">edit</a>

    {{ include('type_event/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}
