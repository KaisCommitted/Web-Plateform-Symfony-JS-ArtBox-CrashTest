<form method="post" action="{{ path('partenaire_delete', {'idPart': partenaire.idPart}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partenaire.idPart) }}">
    <button class="btn">Delete</button>
</form>
