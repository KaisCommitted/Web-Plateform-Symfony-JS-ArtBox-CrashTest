<form method="post" action="{{ path('annonce_delete', {'idAnn': annonce.idAnn}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce.idAnn) }}">
    <button class="btn">Delete</button>
</form>
