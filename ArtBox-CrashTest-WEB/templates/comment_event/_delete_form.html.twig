<form method="post" action="{{ path('comment_event_delete', {'id': comment_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment_event.id) }}">
    <button class="btn">Delete</button>
</form>
