<form method="post" action="{{ path('categorie_delete', {'categorieName': categorie.categorieName}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.categorieName) }}">
    <button class="btn">Delete</button>
</form>
