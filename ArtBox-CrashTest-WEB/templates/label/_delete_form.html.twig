<form method="post" action="{{ path('label_delete', {'id': label.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ label.id) }}">
    <button class="btn">Delete</button>
</form>
