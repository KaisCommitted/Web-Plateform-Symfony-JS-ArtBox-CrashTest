<form method="post" action="{{ path('interactions_delete', {'idInter': interaction.idInter}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interaction.idInter) }}">
    <button class="btn">Delete</button>
</form>
